## 命令名称

命令的简洁说明（用一句话说明该命令的作用）。

<!-- 
使用模板时的注意事项：
- 必需部分：标题、说明、用法、基本示例、与 Claude 协作、注意事项
- 可选部分：选项、详细功能、输出示例、最佳实践、相关命令
- 请根据命令的复杂性仅使用必要的部分
-->

### 用法

```bash
/command-name [选项]
```

### 选项

- 无：默认行为
- `--option1`：选项 1 的说明
- `--option2 <值>`：带参数的选项的说明
- `--flag`：标志选项的说明

※ 如果命令不需要选项，则可以省略整个此部分。

### 基本示例

```bash
# 基本用法
/command-name
“执行时的说明”

# 带选项的示例
/command-name --option1
“使用选项 1 时的说明”

# 带参数选项的示例
/command-name --option2 value
“带参数选项的用法示例”

# 组合多个选项
/command-name --option1 --flag
“组合多个选项的示例”
```

### 详细功能（可选）

#### 功能类别 1

功能的详细说明和使用场景的说明。

```bash
# 具体的代码示例
command example
```

#### 功能类别 2

另一个功能的详细说明。

- **特点 1**：说明
- **特点 2**：说明
- **特点 3**：说明

※ 如果是简单的命令，则可以省略此部分。

### 输出示例（可选）

```
预期的输出格式示例
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

实际输出内容的示例
├─ 项目 1：值
├─ 项目 2：值
└─ 项目 3：值
```

※ 如果输出简单或在基本示例中已充分说明，则可以省略此部分。

### 与 Claude 协作

```bash
# 与文件结合使用的示例
cat file.txt
/command-name
“请求包含文件内容的分析”

# 在管道中使用的示例
command1 | command2
/command-name
“使用前一个命令的输出进行处理”

# 复杂分析的示例
ls -la && cat config.json
/command-name --option1
“组合多个信息进行分析”
```

### 注意事项

- **前提条件**：执行命令所需的前提条件
- **限制**：功能的限制或需要注意的点
- **建议**：有效使用的建议

### 最佳实践（可选）

1. **原则 1**：建议的用法说明
2. **原则 2**：应避免的用法说明
3. **原则 3**：关于性能或效率的建议
4. **原则 4**：关于安全性或可靠性的建议

※ 如果是简单的命令且没有特别的注意事项，则可以省略此部分。

### 相关命令（可选）

- `/related-command1`：具有相关功能的命令
- `/related-command2`：组合使用效果更佳的命令
- `/related-command3`：可作为替代方案的命令

※ 如果没有相关的命令，则可以省略此部分。